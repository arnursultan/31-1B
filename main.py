def calculator():
    print("Простой калькулятор")
    print("Доступные операции: +  -  *  /")

    while True:  # бесконечный цикл, пока пользователь не выйдет
        try:
            # 1. Ввод первого числа
            num1 = float(input("Введите первое число: "))

            # 2. Ввод операции
            operation = input("Введите операцию (+, -, *, /): ")

            # 3. Ввод второго числа
            num2 = float(input("Введите второе число: "))

            # 4. Проверка и выполнение операции
            if operation == "+":
                result = num1 + num2
            elif operation == "-":
                result = num1 - num2
            elif operation == "*":
                result = num1 * num2
            elif operation == "/":
                if num2 != 0:  # проверка деления на ноль
                    result = num1 / num2
                else:
                    print("❌ Ошибка: деление на ноль!")
                    continue
            else:
                print("❌ Неверная операция! Используйте +, -, *, /")
                continue

            # 5. Вывод результата
            print(f"✅ Результат: {result}\n")

        except ValueError:
            print("❌ Ошибка: введите корректные числа!")

        # 6. Спрашиваем, хочет ли пользователь продолжить
        again = input("Хотите продолжить? (да/нет): ").lower()
        if again != "да":
            print("🔚 Калькулятор завершил работу.")
            break


# Запуск программы
calculator()
🔹 Как работает программа
Программа приветствует пользователя и показывает доступные операции.

Пользователь вводит первое число, операцию и второе число.

В зависимости от введённого символа (+, -, *, /) выполняется нужное действие.

Если операция неверная → выводится ошибка.

Если деление на ноль → программа предупреждает и предлагает ввести новые данные.

После каждого действия спрашивает: продолжить или выйти.

Хочешь, я сделаю версию с меню (1 — сложение, 2 — вычитание и т.д.)